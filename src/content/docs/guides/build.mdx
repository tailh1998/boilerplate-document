---
title: Build
description: Build - My pet project.
---

## Prebuild

Clean up the `/build` folder with:

```bash
yarn prebuild
```

or: Remove the `/build` directory from the project manually

## Build

To build the project, run:

```bash
yarn build
```